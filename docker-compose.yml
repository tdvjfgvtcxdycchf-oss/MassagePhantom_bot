services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "88:88"
    volumes:
      - ./data:/app/data
      - ./ssl:/app/ssl:ro
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "8443:8443"
    volumes:
      - ./data:/app/data
      - ./ssl:/app/ssl:ro
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
